# Manual QA - v0.1.6 Linked Groups

- Create or identify a Group.
- Add it to two Workspaces as a linked Group.
- Edit the source Group and confirm both Workspaces resolve the updated Group in preview.
- Add the same source as a detached copy.
- Edit the original Group and confirm detached copy remains independent.
- Delete or corrupt a referenced Group in a test environment and confirm missing reference UI appears instead of crashing.
- Export settings and confirm protected Group names are redacted.
