# Workspace-Aware Placement Planner v0.1.8 Preview

The placement planner can enrich existing recommendations with local Workspace context:

- Used in current Workspace.
- Used only outside the current Workspace.
- Used through linked or detached Groups.
- Unassigned.

The planner remains advisory. It does not move menu bar items unless the separate explicit Experimental Assisted Move path is used.
