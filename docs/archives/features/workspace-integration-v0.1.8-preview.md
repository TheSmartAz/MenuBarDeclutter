# Workspace Integration v0.1.8 Preview

Workspace Integration connects local Workspaces to New Item review, Find Icon, placement hints, crowded rescue, Function Bar, Info Strip, diagnostics, and import/export.

It indexes MenuBarDeclutter-owned Workspace references and Group references. It does not inspect pixels, capture the screen, call private APIs, use the network, or move real menu bar items automatically.
