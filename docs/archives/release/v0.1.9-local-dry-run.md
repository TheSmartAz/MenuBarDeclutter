# v0.1.9 Local Dry Run

Use:

```sh
scripts/build_release.sh --dry-run
scripts/build_release.sh --dry-run --install --verify-installed
```

Expected artifact names:

- `MenuBarDeclutter-v0.1.9-alpha.zip` for dry-run packaging.
- `MenuBarDeclutter-v0.1.9.zip` for non-dry-run local packaging.

Dry-run artifacts are intentionally not notarized.
