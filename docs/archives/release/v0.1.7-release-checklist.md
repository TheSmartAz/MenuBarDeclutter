# v0.1.7 Release Checklist

Post-merge checkpoint: 2026-07-02 on `main` commit `b8ed959`.

- [x] App reports marketing version `0.1.7` and build `8`.
- [x] Info Strip Preview setting defaults off.
- [x] Per-Workspace Info Strip config defaults off.
- [x] Required local tile providers are registered.
- [x] Info Strip rotation skips unavailable providers.
- [x] Function Bar and Info Strip show/hide/toggle through the shared display coordinator.
- [x] Idle-to-Info Strip requires global and per-Workspace opt-in.
- [x] Hover-to-Function Bar requires global and per-Workspace opt-in.
- [x] Status menu opens Info Strip only under Preview/Advanced conditions.
- [x] Diagnostics/export remain privacy-safe.
- [x] No online/media/news/weather/stocks widgets are present.
- [x] Canonical build/test, privacy verification, release dry-run, and installed-app verification are recorded.
- [x] Single-screen UI QA is recorded for Workspaces Preview and Info Strip Preview on the built-in display.
- [x] External multi-display QA is tracked as deferred follow-up, not a current release blocker.

Evidence:

- `scripts/qa_preflight.sh`: PASS on merged `main`; build-for-testing succeeded, 538 app-unit tests in 75 suites passed, 16 UI tests passed, and privacy boundary source checks passed.
- `scripts/build_release.sh --dry-run --install --verify-installed`: PASS on merged `main`; `MenuBarDeclutter-v0.1.7-alpha.zip` and `MenuBarDeclutter-v0.1.7.zip` were produced, artifact verification passed, install to `/Applications/MenuBarDeclutter.app` succeeded, and installed-app verification passed as `0.1.7 (8)`.
- Dry-run distribution remains intentionally non-notarized; `spctl` rejection and missing stapled ticket warnings are expected for this local alpha artifact.
- Current v0.1.7 UI QA scope is single-screen behavior on the built-in display. External multi-display QA remains deferred follow-up.
