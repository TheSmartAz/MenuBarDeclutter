# v0.1.10 Local Dry Run

Use:

```sh
scripts/build_release.sh --dry-run
scripts/build_release.sh --dry-run --install --verify-installed
```

Expected artifact names:

- `MenuBarDeclutter-v0.1.10-alpha.zip` for dry-run packaging.
- `MenuBarDeclutter-v0.1.10.zip` only for explicit non-dry-run packaging, such as the deferred Developer ID path.

Dry-run artifacts are intentionally not notarized. The current packaging script
does not create the non-alpha/versioned zip copy during the default dry-run
commands unless `VERSIONED_ZIP_PATH` or `DRY_RUN=0` is set explicitly.

## Phase 23 Result

Run date: 2026-07-03

Environment:

- Hardware: MacBook Pro, Mac16,7, Apple M4 Pro, 48 GB memory.
- macOS: 26.1 build 25B78.

Results:

| Command | Result | Notes |
| --- | --- | --- |
| `scripts/build_release.sh --dry-run` | PASS | Release archive, dry-run export, package, and artifact verification completed. |
| `scripts/build_release.sh --dry-run --install --verify-installed` | PASS | Final rerun after accessibility/menu polish rebuilt, exported, packaged, installed `/Applications/MenuBarDeclutter.app`, and verified the installed bundle. |

Artifacts:

- Archive: `build/Archives/MenuBarDeclutter.xcarchive`
- Exported app: `build/Export/MenuBarDeclutter.app`
- Dry-run package: `build/Dist/MenuBarDeclutter-v0.1.10-alpha.zip`
- Versioned package copy: not created by the default dry-run packaging flow.
- Installed app: `/Applications/MenuBarDeclutter.app`

Final artifact timestamps:

- `/Applications/MenuBarDeclutter.app`: 2026-07-03 04:41:44 PDT
- `build/Dist/MenuBarDeclutter-v0.1.10-alpha.zip`: 2026-07-03 04:41:45 PDT

Verification notes:

- Installed app reports marketing version `0.1.10` and build `11`.
- Bundle identifier is `Yongjun-Zhang.MenuBarDeclutter`.
- `LSUIElement` and `menubardeclutter` URL scheme are present.
- Screen Recording, Apple Events, and Input Monitoring usage strings are absent.
- No network entitlements are present.
- Executable does not link ScreenCaptureKit.
- Hardened runtime metadata is present.
- `spctl` rejection and missing stapler ticket warnings are expected for the non-notarized dry-run path.
- Persistent local `spctl` resource errors such as `Too many open files` are treated as dry-run infrastructure warnings only after retries and a temporary-copy assessment path. The `--require-notarized` verification path remains strict.
