# Linked Groups Support

Linked Group means a Workspace item references an existing Group. If that Group changes, every Workspace using the linked reference resolves the updated Group.

Detached Copy means MenuBarDeclutter creates a new Group copied from the source. Future edits to the original Group do not update the detached copy.

Use linked references for reusable sets. Use detached copies for one-off Workspace-specific variations.
