# Linked Groups v0.1.9 Preview

Linked Groups can be reused across Workspaces. Detached copies create Workspace-specific snapshots.

Workspace Integration indexes linked and detached references separately so diagnostics, Find Icon badges, and placement hints can explain how an item is used.
