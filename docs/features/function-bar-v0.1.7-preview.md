# Function Bar v0.1.7 Preview

Function Bar remains Preview in v0.1.7 and now shares display coordination with Info Strip.

When Info Strip auto-show is enabled and the active Workspace enables Info Strip, Function Bar can idle into Info Strip. Hovering Info Strip can return to Function Bar when both global and per-Workspace hover settings allow it.
