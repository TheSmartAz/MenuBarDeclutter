# Info Strip v0.1.9 Preview

Info Strip is app-owned Preview UI for lightweight local Workspace status.

It does not use Screen Recording, ScreenCaptureKit, network widgets, notification scraping, or pixel capture. It can hand off to Function Bar when enabled by Workspace and global Preview settings.
