# Workspaces v0.1.7 Preview

v0.1.7 connects Workspaces to Function Bar, Set Builder, and Info Strip Preview.

Workspaces store app-owned Function Bar items and per-Workspace Info Strip configuration. They still do not replace the macOS menu bar, move icons, apply physical profiles automatically, or perform bulk icon moving.
